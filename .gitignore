# NIDQC — .gitignore
# Xem docs/standards/GIT_WORKFLOW.md §5

# --- Dependency quản lý bởi Composer ---
# composer.lock PHẢI được commit; những thứ dưới đây dựng lại từ nó.
/vendor/
/web/core/
/web/modules/contrib/
/web/themes/contrib/
/web/profiles/contrib/
/web/libraries/
/drush/Commands/contrib/

# --- Secrets — TUYỆT ĐỐI KHÔNG commit ---
# (.ddev/.gitignore và web/sites/default/.gitignore cũng chặn một số mục này.
#  Lặp lại ở đây có chủ đích: không phụ thuộc gitignore lồng nhau, chúng có thể bị sửa.)
#
# LƯU Ý: settings.php CÓ ĐƯỢC COMMIT — xem TASK-002 và BACKEND_ARCHITECTURE.md §6.
# Đó là chỗ duy nhất đặt được config_sync_directory, và DDEV cũng mong đợi như vậy.
# settings.php không chứa secret (hash_salt = ''); secret nằm ở 2 file dưới đây.
/web/sites/*/settings.local.php
/web/sites/*/settings.ddev.php
/web/sites/*/services.yml
/web/sites/*/services.local.yml
!/web/sites/default/default.settings.php
!/web/sites/default/default.services.yml
.env
.env.*
!.env.example
*.key
*.pem
*.p12

# Chứng chỉ TLS local do DDEV sinh (có private key)
/.ddev/traefik/certs/

# --- Nội dung người dùng tải lên ---
/web/sites/*/files/
/web/sites/*/private/

# --- Frontend ---
/frontend/node_modules/
/frontend/dist/
/web/themes/custom/nidqc/dist/

# --- DDEV (phần tự sinh) ---
/.ddev/.global_commands/
/.ddev/db_snapshots/
/.ddev/mutagen/mutagen.yml
/.ddev/.dbimageBuild
/.ddev/.webimageBuild
/.ddev/.sshimageBuild
/.ddev/config.local.yaml
/.ddev/*.local.yaml

# --- Hệ điều hành / IDE ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*~

# --- Tạm ---
/scratchpad/
*.log

# Vue SPA build output (ADR-003) — build từ frontend/, không commit
/web/app/
